# 数字余生 · board 开发约定

本目录是黑客松项目的实体打字机链路。`host/` 是 PC 上位机，`espidf/` 是独立 Git 仓库中的 ESP32-S3 固件；相邻 `evo-backend/` 负责 Agent 与打印编排，`web/` 负责浏览器交互。修改前按这一链路定位问题，不把本仓库描述成独立的通用硬件产品。

- 先读根目录 README、相关模块和 `espidf/README.md`；协议细节可参考 `host/docs/backend-handoff.md`，但其中的联调结论可能早于当前代码，以实现和实测为准。
- 改动 `host/` 时保持 `protocol.py` 的帧格式与 `bridge.py`、`api.py` 的请求 ID、状态和重连语义一致。HTTP 接受请求、串口入队、软件队列排空与纸面打印完成是不同事实；不可合并成一个“成功”。
- 改动 `espidf/` 时先检查该内层仓库状态。复用现有 ESP-IDF 组件和 KX-R60 接口；不要凭未确认的引脚、供电、COM 号或 ACK 行为修改接线/时序，也不要把旧版控制台命令写成当前固件的运行方式。
- 保留用户未提交的修改；只改当前任务涉及的文件。运行自动测试可用 `python -m unittest discover -s "host" -p "test_*.py"`，硬件相关结论要与实际接线、串口和纸面验证分开报告。
- 代码注释沿用邻近文件语言。未明确要求时，不执行烧录、实际打印、推送或历史改写。
